{
  "name": "neetdiv_form",
  "version": "1.0.0",
  "description": "League of Legends summoner information management system for Google Apps Script",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.js --bundle --outfile=dist/Code.gs --format=esm --platform=browser --target=es2020",
    "build:watch": "npm run build -- --watch",
    "build:dev": "npm run build -- --sourcemap",
    "format": "prettier -w **/*.{json,md,yml} && biome check --write",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest --coverage",
    "test:ui": "vitest --ui"
  },
  "author": "Community",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@types/google-apps-script": "^1.0.100",
    "esbuild": "^0.25.8",
    "lefthook": "^1.12.3",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
